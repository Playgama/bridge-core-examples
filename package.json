{
    "name": "bridge-core-examples",
    "private": true,
    "version": "0.1.0",
    "type": "module",
    "description": "Full TypeScript test bench exercising every module of the Playgama Bridge SDK.",
    "scripts": {
        "dev": "vite",
        "build": "tsc --noEmit && vite build",
        "preview": "vite preview",
        "typecheck": "tsc --noEmit"
    },
    "devDependencies": {
        "typescript": "^5.6.3",
        "vite": "^5.4.10"
    },
    "dependencies": {
        "@fontsource/jetbrains-mono": "^5.2.8"
    }
}
